<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <title>Spinning playlist</title>

    <link rel='shortcut icon' href='img/favicon.ico' type='image/x-icon'>
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
    <link href="css/main.css" rel="stylesheet">
  </head>
  <body>
    <div id="container">
      <h1>playlist</h1>
      <p class="instructions">Click the CD to play or pause the music for each song</p>
      <p class="info">Deezer only allows 30 second previews for users who aren't logged in :'( </p>

      <section id="music-container">
      <% unless songs.nil? %>
        <% songs.each do |song| %>
            <div class="song"
                 name="<%= song['title'] %>"
                 id="<%= song['id']%>">

              <p class="title">
                <%= song['title'] %>,
                <%= song['contributors'].first['name'] %>
              </p>
              <audio src="<%= song['preview'] %>"
                     class="<%= song['id']%>">
                <p>Your browser does not support the <code>audio</code> element </p>
              </audio>

              <img class="music-controls" src="img/CD_icon.png" >

            </div>
        <% end %>
      <% else %>
          <p>Sorry these Deezer songs are not available at the minute</p>
      <% end %>
      </section>
     <script src="js/play.js" type="text/javascript"></script>

    </div>
  </body>
</html>

