<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" name="viewport">
    <title>Crazy playlist</title>
    <link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
    <link href="css/main.css" rel="stylesheet">
  </head>
  <body>
    <div id="container">
      <h1>playlistsssss</h1>
      <p>Deezer only allows 30 second previews for users who aren't logged in :'( </p>

      <section id="music-container">
      <% unless songs.nil? %>
        <% songs.each do |song| %>
            <div class="song"
                 name="<%= song['title'] %>"
                 id="<%= song['id']%>">

              <p>
                <%= song['title'] %>,
                <%= song['contributors'].first['name'] %>
              </p>

              <img class="music-controls"  src="img/CD_icon.png">

              <audio src="<%= song['preview'] %>"
                     controls
                     class="<%= song['id']%>">
                <p>Your browser does not support the <code>audio</code> element </p>
              </audio>

            </div>
        <% end %>
      <% else %>
          <p>Sorry these Deezer songs are not available at the minute</p>
      <% end %>
      </section>
     <script src="js/play.js" type="text/javascript"></script>

    </div>
  </body>
</html>

